<h2>Resend unlock instructions</h2>

<%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email", "aria-label": "Email address for unlock instructions" %>
  </div>

  <div class="actions">
    <%= f.submit "Resend unlock instructions", "aria-label": "Resend account unlock instructions to email" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
